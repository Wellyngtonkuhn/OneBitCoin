import { StyleSheet } from "react-native";



const styles = StyleSheet.create({
    headerPrice: {
        width: '100%',
        height: 'auto',
        alignItems: 'center',
        paddingBottom: 15,
    },

    currentPrice: {
        color: '#f50d41',
        fontSize: 42,
        fontWeight: 'bold',
        paddingTop: 20,
    },

    textPrice: {
        color: '#fff',
        fontSize: 16,
    },
})

export default styles